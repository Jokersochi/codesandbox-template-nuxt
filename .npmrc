# Set yarn as the default package manager
# This ensures that 'npm install' or 'pnpm install' will not be used by mistake.
# This is especially useful for templates where the package manager is already decided.
